# Copy to .env for local runs (.env is gitignored — never commit real keys).
# In CI these come from GitHub Actions repository secrets of the same name.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
FIREWORKS_API_KEY=
DEEPSEEK_API_KEY=
